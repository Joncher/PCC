import React, { Component } from "react";
import { Container } from "semantic-ui-react";

class AboutSection extends Component {
  render() {
    return <Container height={this.props.height}>About</Container>;
  }
}

export default AboutSection;
